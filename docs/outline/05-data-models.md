# Data Models

All data models are defined in `lib/data/models/` and map directly to Cloud Firestore collections. Each model includes serialization/deserialization methods, validation, and default values.

## User Roles & Status

_(To be filled)_
