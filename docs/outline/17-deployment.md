# Deployment & Configuration

## Firebase Configuration

_(To be filled)_

## Platform Configuration

_(To be filled)_

## Environment Variables

_(To be filled)_

## Build Commands

- `flutter pub get` — Install dependencies
- `flutter analyze` — Static analysis
- `flutter test` — Run test suite
- `flutter build apk --release` — Build Android APK
- `flutter build web` — Build web application
- `firebase deploy` — Deploy to Firebase Hosting and Firestore rules

## Linting

The `analysis_options.yaml` (64 lines) enforces strict rules across Style, Correctness, Security, Maintainability, and Dart 3.x categories to ensure code quality and consistency.
